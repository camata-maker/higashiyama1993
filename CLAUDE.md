# 50歳同窓会サイト（higashiyama1993）

- Cloudflare Workersの静的サイト。デプロイは `wrangler deploy`（wrangler.toml参照）。
- GitHubリモート: camata-maker/higashiyama1993。
- 画像・フォントは `assets/` 配下に整理済み。除外対象は `.assetsignore` で管理。
- `旧試作/` は初期プロトタイプ（参考用、本番コードではない）。
- `ギャラリー写真/` に `_非公開` サブフォルダあり。外部公開する処理では絶対に含めない。
